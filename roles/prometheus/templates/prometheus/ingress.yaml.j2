 apiVersion: extensions/v1beta1
 kind: Ingress
 metadata:
   name: grafana
   namespace: {{ prometheus.namespace }}
 spec:
   rules:
   - host: {{ grafana.grafana_url }}
     http:
       paths:
       - path: /
         backend:
          serviceName: grafana
          servicePort: 3000
